# دليل تثبيت نظام الصلاحيات (RBAC)

## نظرة عامة

هذا الدليل يشرح كيفية تثبيت وتكوين نظام الصلاحيات المتكامل (Role-Based Access Control) لنظام إدارة الجراج.

## 🔴 المتطلبات الأساسية

### الشرط الأول: المستخدمون الأصليون
- **المستخدمون الأربعة الحاليون** سيتم ترقيتهم تلقائياً إلى **Admin**
- لا يمكن تغيير صلاحياتهم أو حذفهم
- يتم تمييزهم في لوحة التحكم بعلامة ⭐

### الشرط الثاني: المستخدمون الجدد
- أي مستخدم جديد سيحصل تلقائياً على صلاحية **Viewer**
- يمكن للأدمن فقط ترقيتهم إلى Manager أو Admin

## 📋 خطوات التثبيت

### الخطوة 1: تحديث عناوين المستخدمين الأصليين

1. افتح الملف: `scripts/upgrade-existing-users.js`
2. قم بتحديث قائمة `EXISTING_USERS` بالبيانات الصحيحة:

```javascript
const EXISTING_USERS = [
    { email: 'user1@example.com', name: 'أحمد' },
    { email: 'user2@example.com', name: 'محمد' },
    { email: 'user3@example.com', name: 'سارة' },
    { email: 'user4@example.com', name: 'فاطمة' }
];
```

### الخطوة 2: تثبيت Firebase Admin SDK

```bash
cd scripts
npm install
```

### الخطوة 3: الحصول على مفتاح الخدمة

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك
3. اذهب إلى Project Settings > Service Accounts
4. انقر على "Generate New Private Key"
5. احفظ الملف باسم `serviceAccountKey.json` في مجلد `scripts`

### الخطوة 4: تشغيل سكريبت الترقية

```bash
cd scripts
node upgrade-existing-users.js
```

### الخطوة 5: نشر Cloud Functions

```bash
firebase deploy --only functions
```

### الخطوة 6: تحديث Firestore Rules

```bash
firebase deploy --only firestore:rules
```

## 🎯 الأدوار والصلاحيات

### 👑 Admin (المشرف)
- **الوصول الكامل** لجميع الوظائف
- إدارة المستخدمين والأدوار
- عرض وتعديل وحذف جميع البيانات
- الوصول إلى لوحة التحكم

### 📊 Manager (المدير)
- **إضافة وعرض** البيانات
- **لا يمكنه الحذف**
- لا يمكن تعديل السجلات المعتمدة
- تصدير التقارير

### 👁️ Viewer (المشاهد)
- **قراءة فقط** (الدور الافتراضي للمستخدمين الجدد)
- عرض البيانات والتقارير
- طباعة التقارير

## 📁 الملفات الجديدة

### ملفات JavaScript
- `js/auth-guard.js` - حماية المسارات والتحقق من الصلاحيات
- `js/ui-restrict.js` - تقييد عناصر الواجهة حسب الدور
- `js/role-manager.js` - إدارة الأدوار والمستخدمين
- `js/user-management.js` - واجهة إدارة المستخدمين
- `js/rbac-main.js` - التهيئة الرئيسية للنظام

### ملفات HTML
- `admin-panel.html` - لوحة تحكم المشرف

### ملفات CSS
- `css/admin-panel.css` - أنماط لوحة التحكم

### ملفات Configuration
- `firestore-rbac.rules` - قواعد أمان Firestore
- `scripts/upgrade-existing-users.js` - سكريبت ترقية المستخدمين

## 🔧 التكوين

### إضافة سطر واحد لكل صفحة HTML

أضف هذا السطر في نهاية كل صفحة HTML (قبل `</body>`):

```html
<script type="module" src="./js/rbac-main.js"></script>
```

### تحديث الصفحات المحددة

الصفحات التالية تم تحديثها بالفعل:
- `index.html` - صفحة تسجيل الدخول
- `dashboard.html` - لوحة التحكم الرئيسية

## 🚀 كيفية الاستخدام

### للمستخدمين الأصليين
1. سجل دخولك بكلمة المرور الحالية
2. ستحصل تلقائياً على صلاحية Admin
3. يمكنك الوصول إلى جميع الوظائف

### للمدير (Admin)
1. سجل دخولك
2. اذهب إلى لوحة التحكم (رابط "⚙️ لوحة التحكم")
3. إدارة المستخدمين والأدوار
4. عرض سجل التغييرات (Audit Trail)

### للمستخدمين الجدد
1. سجل دخولك
2. ستحصل تلقائياً على صلاحية Viewer
3. يمكنك عرض البيانات المتاحة فقط
4. تواصل مع Admin للحصول على صلاحيات إضافية

## 🔒 ميزات الأمان

### حماية المستخدمين الأصليين
- لا يمكن تغيير دورهم عن Admin
- لا يمكن حذفهم من النظام
- يتم تمييزهم بعلامة ⭐ في لوحة التحكم

### Audit Trail
- تسجيل جميع تغييرات الأدوار
- تسجيل إنشاء وحذف المستخدمين
- معلومات كاملة عن من قام بالتغيير ومتى

### Route Guards
- حماية الصفحات حسب الصلاحية
- إعادة توجيه تلقائي لصفحة تسجيل الدخول
- رسائل خطأ واضحة عند رفض الوصول

## 🎨 واجهة المستخدم

### مؤشرات الدور
- 👑 Admin - لون أحمر
- 📊 Manager - لون برتقالي
- 👁️ Viewer - لون أخضر

### قيود الواجهة
- تعطيل الأزرار غير المسموح بها
- إخفاء القوائم المحمية
- رسائل توضيحية عند محاولة الوصول المرفوض

## 🔧 اختصارات لوحة المفاتيح

- `Ctrl+Shift+L` - تسجيل الخروج
- `Ctrl+Shift+A` - لوحة التحكم (للمشرفين فقط)
- `Ctrl+E` - تصدير البيانات (في صفحة إدارة المستخدمين)
- `Ctrl+F` - البحث عن المستخدمين

## 🐛 استكشاف الأخطاء

### مشاكل شائعة

1. **لم يتم ترقية المستخدمين الأصليين**
   - تأكد من تشغيل سكريبت `upgrade-existing-users.js`
   - تحقق من صحة عناوين البريد الإلكتروني
   - تأكد من وجود `serviceAccountKey.json`

2. **صفحة لا تفتح**
   - تأكد من إضافة `rbac-main.js` للصفحة
   - تحقق من صلاحياتك في Firestore Rules
   - افحص console للمزيد من المعلومات

3. **الأزرار لا تعمل**
   - تحقق من صلاحياتك الحالية
   - تأكد من تهيئة النظام بشكل صحيح
   - أعد تحميل الصفحة

### التحقق من النظام

```javascript
// في console المتصفح
validateRBACSystem();
```

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من console المتصفح للأخطاء
2. تأكد من اتباع جميع خطوات التثبيت
3. تحقق من إعدادات Firebase

## 🔄 التحديثات

لتحديث النظام:
1. تحديث ملفات JavaScript
2. نشر Cloud Functions الجديدة
3. تحديث Firestore Rules

---

**ملاحظة هامة:** قم دائماً بأخذ نسخة احتياطية من البيانات قبل تشغيل سكريبت الترقية!
