# Garage System Test Case

1. سجل الدخول باستخدام مستخدم يملك `manager` أو `admin`.
2. افتح الصفحة [garage_system.html](/C:/Users/PC%202/Desktop/trialsafapricess2026/safaprices2026/garage_system.html).
3. من تبويب `البيانات الأساسية`:
   أضف حساباً محاسبياً للمخزون وحساب مصروف صيانة وحساب خردة.
4. أضف صنفاً جديداً:
   مثال: فلتر زيت، حد أدنى `2`، سعر وحدة `350`.
5. أضف مركبة:
   مثال: `س ط ر 1234`، موديل `Volvo FH`, عميل `SAFATRANS`.
6. أضف أمر صيانة جديد للمركبة.
7. من تبويب `حركات المخزون` نفذ حركة `IN` للصنف بكمية `10` وتكلفة `3500`.
   النتيجة المتوقعة:
   يزيد `currentStock` إلى `10`.
   يتم إنشاء حركة مخزنية وقيد محاسبي مزدوج.
8. افتح عهدة بقيمة `5000`.
9. نفذ حركة `OUT` بكمية `2` وتكلفة `700` واربطها بالمركبة وأمر الصيانة والعهدة.
   النتيجة المتوقعة:
   ينخفض `currentStock` إلى `8`.
   يتم تحديث `totalCost` على أمر الصيانة.
   ينخفض `remaining` للعهدة بمقدار `700`.
   يتم إنشاء `journal_entries` من خلال Cloud Function فقط.
10. سجّل استبدالاً مع خردة:
    اختر القطعة الجديدة نفسها، وحدد قطعة خردة قديمة، وكمية `1` وتكلفة قطعة جديدة `350` وقيمة خردة `100`.
    النتيجة المتوقعة:
    يتم تسجيل حركة صرف للقطعة الجديدة.
    يتم إنشاء حركة `SCRAP` آلية مرتبطة بها.
11. انتقل إلى تبويب `التقارير`:
    أصدر تقرير ربحية السيارة.
    أصدر كشف حساب العهدة.
    أصدر تقرير الجرد الدوري.
12. حاول صرف كمية أكبر من الرصيد.
    النتيجة المتوقعة:
    ترفض العملية برسالة خطأ بدون تغيير الأرصدة.
13. حاول تسوية عهدة ما زال فيها `remaining > 0`.
    النتيجة المتوقعة:
    ترفض Cloud Function عملية التسوية.
